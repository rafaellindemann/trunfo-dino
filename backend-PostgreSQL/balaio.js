alert("Idade: " + idadeUsuario)

let mensagem = "Idade: " + idadeUsuario // concatenação

let msg = `Idade: ${idadeUsuario}`

('SELECT * FROM dinos WHERE id_dino = $1', [id]);
('SELECT * FROM dinos WHERE id_dino = $1 $2', [id, nome]);